apiVersion: apps/v1
kind: Deployment
metadata:
  name: github-actions-stocks
spec:
  replicas: 1
  selector:
    matchLabels:
      app: github-actions-stocks
  template:
    metadata:
      labels:
        app: github-actions-stocks
    spec:
      containers:
        - name: app
          image: gcr.io/PROJECT_ID/IMAGE:TAG
          imagePullPolicy: Always